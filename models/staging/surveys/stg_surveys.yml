version: 2

models:
  - name: stg_surveys__survey
    description: one unique response per row
    columns: 
      - name: survey_responseid
        description: primary key
        tests:
          - unique
          - not_null